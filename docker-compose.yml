services:
  postgres:
    image: postgres:16-alpine
    container_name: combinado-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: combinado_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d combinado_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
